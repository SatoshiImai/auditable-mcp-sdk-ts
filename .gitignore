node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store

# VSCode-remote noise not needed in this repo
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# Work-in-progress notes — local only, as in the Python SDK and the spec repo.
wip/
